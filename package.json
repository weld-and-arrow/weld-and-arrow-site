{
  "name": "weld-and-arrow-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:context": "node scripts/build-context.mjs",
    "fetch:pricing": "node scripts/fetch-pricing.mjs",
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "typescript": "^5.5.4",
    "wrangler": "^4.24.0"
  }
}
